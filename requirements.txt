# ============================================================
# Old photo restoration system — modern stack dependencies (Python 3.11)
#
# Windows installation steps:
#   1) conda create -n fixoldimg-gpu python=3.11 -y
#   2) pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 \
#        --index-url https://download.pytorch.org/whl/cu128
#      (China mirror: https://mirror.sjtu.edu.cn/pytorch-wheels/cu128)
#   3) pip install -r requirements.txt
#   4) conda install -n fixoldimg-gpu -c conda-forge dlib=20.0.1
# ============================================================

# ---- Web layer ----
gradio==6.22.0
fastapi==0.141.1
uvicorn==0.52.1
python-multipart

# ---- Scientific computing / imaging ----
numpy==2.4.6
Pillow==12.3.0
opencv-python==5.0.0.93
scipy==1.17.1
scikit-image==0.26.0
matplotlib==3.11.1
pyspng==0.1.4
timm==0.9.2        # DDColor colorization module (ConvNeXt layers)

# ---- Inference ----
# torch / torchvision / torchaudio are installed from the cu128 index in step 2
# (2.7.1+cu128 includes RTX 50 series sm_120 support; GPU is enabled automatically on the web side)
dlib>=20.0.1   # install the prebuilt version from conda-forge
easydict==1.13
dominate==2.9.1
dill==0.4.0
tensorboardX==2.6.2.2
einops==0.8.1

# ---- Basics ----
PyYAML==6.0.3
filelock==3.16.1
