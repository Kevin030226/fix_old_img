[pytest]
testpaths = tests/unit tests/integration tests/gpu
python_files = test_*.py
addopts = -q
markers =
    gpu: requires model weights and/or a CUDA GPU (deselect with -m "not gpu")
    integration: end-to-end API tests over a real temp database
